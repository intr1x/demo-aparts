# 🚀 Деплой на Vercel

## Пошаговая инструкция

### 1. Подготовка базы данных PostgreSQL

Выберите один из провайдеров:

#### Вариант А: Vercel Postgres (рекомендуется) ⭐
1. Перейдите на https://vercel.com/dashboard
2. В разделе Storage → Create → Postgres
3. Создайте новую базу данных
4. Скопируйте `DATABASE_URL` из настроек

#### Вариант Б: Supabase (бесплатный)
1. Перейдите на https://supabase.com
2. Создайте новый проект
3. Скопируйте Connection String из Settings → Database
4. Формат: `postgresql://postgres:[YOUR-PASSWORD]@[HOST]:5432/postgres`

#### Вариант В: Neon (бесплатный)
1. Перейдите на https://neon.tech
2. Создайте новый проект
3. Скопируйте Connection String

### 2. Деплой на Vercel

#### Способ 1: Через веб-интерфейс (проще)

1. Войдите на https://vercel.com
2. Нажмите **"Add New..." → Project**
3. Импортируйте репозиторий `intr1x/aparts`
4. Vercel автоматически определит Next.js настройки
5. Добавьте **Environment Variables**:

```env
PAYLOAD_SECRET=ваш_секретный_ключ_минимум_32_символа
DATABASE_URI=postgresql://user:password@host:5432/database
NEXT_PUBLIC_SERVER_URL=https://ваш-проект.vercel.app
NODE_ENV=production
```

6. Нажмите **"Deploy"**

#### Способ 2: Через Vercel CLI

```bash
# Установите Vercel CLI
npm i -g vercel

# Войдите в аккаунт
vercel login

# Деплой
cd /Users/nikita/Desktop/Projects/Aparts
vercel

# Следуйте инструкциям в терминале
```

### 3. Настройка переменных окружения

После создания проекта на Vercel:

1. Перейдите в **Settings → Environment Variables**
2. Добавьте все переменные из `.env.example`
3. Убедитесь, что `NEXT_PUBLIC_SERVER_URL` указывает на ваш Vercel домен

### 4. Первичная настройка базы данных

После первого деплоя:

1. Зайдите на `https://ваш-проект.vercel.app/admin`
2. Создайте первого пользователя-администратора
3. Payload CMS автоматически создаст необходимые таблицы

Или выполните seed через Vercel CLI:

```bash
# Скачайте переменные окружения
vercel env pull

# Выполните seed
npm run seed
```

### 5. Настройка домена (опционально)

1. В Vercel Dashboard → Settings → Domains
2. Добавьте свой домен (например, `aparts.ru`)
3. Обновите `NEXT_PUBLIC_SERVER_URL` на новый домен
4. Перезапустите deployment

### 6. Настройка хранилища изображений (важно!)

⚠️ **Vercel Serverless не поддерживает файловую систему для загрузки медиа.**

Выберите одно из решений:

#### Вариант А: Vercel Blob Storage (рекомендуется)

```bash
npm install @payloadcms/storage-vercel-blob
```

Обновите `payload.config.ts`:

```typescript
import { vercelBlobStorage } from '@payloadcms/storage-vercel-blob'

export default buildConfig({
  // ... остальные настройки
  plugins: [
    vercelBlobStorage({
      collections: {
        media: true,
      },
      token: process.env.BLOB_READ_WRITE_TOKEN!,
    }),
  ],
})
```

Добавьте в Vercel Environment Variables:
```env
BLOB_READ_WRITE_TOKEN=ваш_токен_из_vercel_blob_storage
```

#### Вариант Б: Cloudflare R2 / AWS S3

Используйте `@payloadcms/storage-s3` для интеграции с S3-совместимыми хранилищами.

### 7. Автоматический деплой

После настройки каждый push в `main` ветку будет автоматически деплоить новую версию:

```bash
git add .
git commit -m "Update content"
git push origin main
```

## 🔍 Проверка деплоя

После деплоя проверьте:

- ✅ Главная страница: `https://ваш-проект.vercel.app/`
- ✅ Админка: `https://ваш-проект.vercel.app/admin`
- ✅ API: `https://ваш-проект.vercel.app/api/apartments`

## 📊 Мониторинг

- **Логи**: Vercel Dashboard → Deployments → Logs
- **Аналитика**: Vercel Dashboard → Analytics
- **Performance**: Vercel Dashboard → Speed Insights

## 🐛 Решение проблем

### Ошибка сборки (Build Error)

```bash
# Проверьте логи в Vercel Dashboard
# Обычно это связано с отсутствующими зависимостями или переменными окружения
```

### База данных не подключается

1. Проверьте `DATABASE_URI` в Environment Variables
2. Убедитесь, что IP Vercel не заблокирован в настройках БД
3. Для Supabase/Neon проверьте, что включен SSL

### Изображения не загружаются

Настройте Vercel Blob Storage (см. раздел 6)

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [Payload CMS Deployment](https://payloadcms.com/docs/production/deployment)
- [Vercel Postgres](https://vercel.com/docs/storage/vercel-postgres)
- [Vercel Blob Storage](https://vercel.com/docs/storage/vercel-blob)

